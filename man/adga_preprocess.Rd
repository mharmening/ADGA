% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{adga_preprocess}
\alias{adga_preprocess}
\title{Preprocess text data for ADGA}
\usage{
adga_preprocess(
  data,
  text_col,
  id_col,
  group_col,
  language,
  pos_filter = c("NOUN", "ADJ", "ADV", "VERB", "PROPN"),
  carry_cols = NULL
)
}
\arguments{
\item{data}{A data frame containing the text to annotate.}

\item{text_col}{Name of the column containing the text (as string).}

\item{id_col}{Name of the column containing a unique response ID (as string).}

\item{group_col}{Name of the column to use as the ADGA group variable (as string).}

\item{language}{UDPipe language model string, e.g. \code{"norwegian-nynorsk"}.}

\item{pos_filter}{Character vector of UPOS tags to keep.
Defaults to \code{c("NOUN", "ADJ", "ADV", "VERB", "PROPN")}.}

\item{carry_cols}{Character vector of additional columns from \code{data} to
carry over to the token table (e.g. sociodemographics). Defaults to \code{NULL}.}
}
\value{
A tibble of annotated tokens with lemmas, POS tags, and metadata.
}
\description{
Annotates a text column using UDPipe (lemmatization, POS-tagging,
tokenization), filters by part-of-speech tags, and merges metadata
back onto the token table.
}
\examples{
\dontrun{
annotated <- adga_preprocess(
  data       = my_data,
  text_col   = "openanswer",
  id_col     = "responseid",
  group_col  = "humanmade",
  language   = "norwegian-nynorsk"
)
}
}
