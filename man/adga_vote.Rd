% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vote.R
\name{adga_vote}
\alias{adga_vote}
\title{Run the ADGA voting model to select keywords}
\usage{
adga_vote(
  tfidf,
  chisquare,
  wordscore,
  goal = 1000,
  i_start = 50,
  i_stop = 2500
)
}
\arguments{
\item{tfidf}{A tibble returned by \code{\link[=adga_tfidf]{adga_tfidf()}}.}

\item{chisquare}{A tibble returned by \code{\link[=adga_chisquare]{adga_chisquare()}}.}

\item{wordscore}{A tibble returned by \code{\link[=adga_wordscore]{adga_wordscore()}}.}

\item{goal}{Integer. Target number of keywords per group. Defaults to \code{1000}.}

\item{i_start}{Integer. Starting size of each ranked input list. Defaults to \code{50}.}

\item{i_stop}{Integer. Maximum input list size before stopping. Defaults to \code{2500}.}
}
\value{
A tibble with one row per group, containing columns \code{group},
\code{i_final} (list size at which goal was reached), \code{n_keywords}, and
\code{keywords} (a list-column of tibbles with lemma, votes, and measure flags).
}
\description{
Combines tf-idf, chi-square, and wordscore rankings through an iterative
voting procedure. Starting from the top \code{i_start} words per measure, a term
is added to the dictionary if it appears in at least two of the three
ranked lists. The lists are expanded by one each iteration until the target
dictionary size is reached.
}
\examples{
\dontrun{
results <- adga_vote(tfidf, chisquare, wordscore, goal = 100)
}
}
