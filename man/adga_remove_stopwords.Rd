% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stopwords.R
\name{adga_remove_stopwords}
\alias{adga_remove_stopwords}
\title{Remove stopwords from ADGA metric tables}
\usage{
adga_remove_stopwords(
  tfidf,
  chisquare,
  wordscore,
  language = "no",
  source = "snowball"
)
}
\arguments{
\item{tfidf}{A tibble returned by \code{\link[=adga_tfidf]{adga_tfidf()}}.}

\item{chisquare}{A tibble returned by \code{\link[=adga_chisquare]{adga_chisquare()}}.}

\item{wordscore}{A tibble returned by \code{\link[=adga_wordscore]{adga_wordscore()}}.}

\item{language}{Language code passed to \code{\link[stopwords:stopwords]{stopwords::stopwords()}},
e.g. \code{"no"} for Norwegian. Defaults to \code{"no"}.}

\item{source}{Stopword source passed to \code{\link[stopwords:stopwords]{stopwords::stopwords()}}.
Defaults to \code{"snowball"}.}
}
\value{
A named list with elements \code{tfidf}, \code{chisquare}, and \code{wordscore},
each with stopwords removed.
}
\description{
Removes stopwords from the output of \code{\link[=adga_tfidf]{adga_tfidf()}}, \code{\link[=adga_chisquare]{adga_chisquare()}},
and \code{\link[=adga_wordscore]{adga_wordscore()}}. Stopwords are loaded via the \code{stopwords} package.
}
\examples{
\dontrun{
cleaned <- adga_remove_stopwords(tfidf, chisquare, wordscore, language = "no")
tfidf     <- cleaned$tfidf
chisquare <- cleaned$chisquare
wordscore <- cleaned$wordscore
}
}
